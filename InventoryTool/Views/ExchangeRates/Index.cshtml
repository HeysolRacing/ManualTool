@model IEnumerable<InventoryTool.Models.ExchangeRate>

@{
    ViewBag.Title = "Index";
}

<h2>FX daily update</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Exchange)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Exchangedate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Exchange)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Exchangedate)
        </td>
        <td>
            @*@Html.ActionLink("Edit", "Edit", new { id=item.ExchangeRateID })*@ 
        </td>
    </tr>
}

</table>
